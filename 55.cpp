// 55. Jump Game
#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    bool canJump(vector<int>& nums) {
        int curPos = 1;
        for (int i = 0; i < nums.size(); i++){
            if nums[curPos]
            curPos = nums[curPos];
            if (curPos == nums.size() - 1)
                return true;
            if (curPos > curPos)
        }
    }
};


// test array
// {3, 2, 1, 0, 4}

int main(){
    return 0;
}