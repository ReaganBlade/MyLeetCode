# 2257. Count Unguarded Cells in the Grid
from typing import List

class Solution:
    def checkCells(self, row, col, grid: )

    def countUnguarded(self, m: int, n: int, guards: List[List[int]], walls: List[List[int]]) -> int:
        